<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Login | AutoNexus</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Bootstrap 5 CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="icon" type="image/png" href="/images/favicon.png" />
    <link rel="stylesheet" href="/stylesheets/login.css" />
  </head>
  <body>
    <!-- Navbar -->
    <%- include('partials/navbar') %>

    <!-- Login Hero Section -->
    <section class="login-hero mb-5">
      <div class="login-hero-overlay"></div>
      <div class="login-hero-content container py-5">
        <h1>Log In to AutoNexus</h1>
        <p class="lead mt-3" style="color: #e0e7ff">
          Access your account and manage your bookings.
        </p>
      </div>
    </section>

    <!-- Login Form Section -->
    <div class="container d-flex justify-content-center">
      <div class="login-section w-100">
        <form class="login-form" action="/login" method="post">
          <div class="mb-3">
            <label for="username" class="form-label">Email</label>
            <input
              type="text"
              name="username"
              id="username"
              required
              placeholder="Enter your email"
            />
          </div>
          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input
              type="password"
              name="password"
              id="password"
              required
              placeholder="Enter your password"
            />
          </div>
          <div class="text-center">
            <button type="submit" class="btn">Sign In</button>
          </div>
        </form>
        <div class="login-links">
          <span>Don't have an account?</span>
          <a href="/signup">Sign Up</a>
          <br />
          <a href="/">Back to Home</a>
        </div>
      </div>
    </div>

    <!-- Footer  -->
    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
